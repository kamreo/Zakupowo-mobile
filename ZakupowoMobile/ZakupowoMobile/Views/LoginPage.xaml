<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ZakupowoMobile.Views.LoginPage"
             Shell.NavBarIsVisible="False">
  
    <ContentPage.Content >
        <Grid BackgroundColor="White"  >

            <Grid RowSpacing="20" Grid.Row="0" Margin="20,100,20,0" >
                <Grid.RowDefinitions >

                    <RowDefinition Height="60"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>

                </Grid.RowDefinitions>
                <Image Source="shopLogo.png" HeightRequest="60"/>
                <Entry Grid.Row="1" Placeholder="Adres email" x:Name ="emailEntry" Text="{Binding Email}" PlaceholderColor="#bababa" FontSize="16"/>
                <Entry Grid.Row="2" Placeholder="Hasło" x:Name ="passwordEntry" Text="{Binding Password}" IsPassword="True" PlaceholderColor="#bababa" FontSize="16"/>
                <Button Text="Zaloguj się" x:Name ="signIn" BackgroundColor="#FFB24A"  CornerRadius="5"  VerticalOptions="Start" Grid.Row="3"  Clicked="signIn_Clicked"/>
                <Label  Grid.Row="4" Text="Nie masz konta? Zarejestruj się."   VerticalOptions="Center" HorizontalTextAlignment="Center">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding RegisterCommand}" />
                    </Label.GestureRecognizers>
                </Label>
            </Grid>

        </Grid>
    </ContentPage.Content>
</ContentPage>